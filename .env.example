# VITE_API_URL — base URL for the API client (src/shared/api/client.ts).
# Defaults to "/api" (same-origin via reverse proxy or Vite dev proxy).
# For cross-origin dev, override with an absolute URL:
#   VITE_API_URL=http://localhost:3000/api
VITE_API_URL=/api
